---
core: true
lastModified: '2026-03-29T06:23:26Z'
tags:
- task
- active
---

# Task Tracker

## Current Task: Планирование первой волны pytest-тестов

## Description
Согласовано, что первая волна покрывает все предложенные базовые механизмы: сущности, чистые операторы, базовые сценарии движка и сериализацию. Следующий шаг — перейти к реализации тестовой инфраструктуры и самих тестов.

## Next Steps
- [ ] Добавить `pytest` в `pyproject.toml` и создать каталог `tests/`.
- [ ] Реализовать фикстуры и вспомогательную `DummyProblem` для детерминированных тестов.
- [ ] Добавить unit-тесты для `ChromosomeSchema`, `Problem`, `Individual`.
- [ ] Добавить unit-тесты для `selection`, `mutation`, `crossover`.
- [ ] Добавить базовые тесты для `Tardigradas` (`__init__`, `population_init`, `new_valid_individual`, `kill_doubles`).
- [ ] Добавить тесты сериализации (`state_dict`, `restore_from_dict`, файловый round-trip`).
- [ ] Прогнать `pytest` и при необходимости поправить код или тесты.

## Blockers
- Нет.

## Open Tasks
- [ ] Реализовать первую волну pytest-тестов базовых механизмов.
- [ ] Добавить тесты для `step()` и `loop()` как следующий слой интеграционного покрытия.
- [ ] Расширить новый API примерами и конфигурируемыми стратегиями.

## Recently Completed
- [x] Согласован объём первой волны: сущности, операторы, движок и сериализация (29.03.2026)
- [x] Собран контекст проекта и выделены зоны для первой волны pytest-тестов (29.03.2026)
- [x] Проведён полный архитектурный рефакторинг пакета без сохранения обратной совместимости (29.03.2026)
- [x] Разделены сущности задачи, особи, схемы хромосомы, движка, операторов и сериализации (29.03.2026)
- [x] Обновлены README и публичный API под новый контракт `Problem`/`Individual` (29.03.2026)
- [x] Проверены импорт, базовый сценарий и сериализация после рефакторинга (29.03.2026)