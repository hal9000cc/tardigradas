---
core: false
lastModified: '2026-03-29T06:17:09Z'
tags:
- progress
- log
---

## 29.03.2026
- [x] Проект переведён на стандартный `src`-layout: код перенесён в `src/tardigradas/`.
- [x] Добавлен `pyproject.toml` с настройкой `setuptools` и зависимостью `numpy`.
- [x] Добавлены базовые служебные файлы (`.gitignore`) и обновлён `README.md`.
- [x] Проверен импорт пакета через `sys.path.insert(..., 'src')` и корректное чтение `pyproject.toml`.

### Примечания
- Логика алгоритма намеренно не рефакторилась.
- Диагностика Pylance по части старого кода сохранена и не исправлялась в рамках задачи.

## 29.03.2026
- [x] Изучен весь текущий проект и подтверждены проблемы, ранее отмеченные в аналитической документации.
- [x] Исправлен дефолтный `loop_fun`: теперь `loop()` по умолчанию использует bound-method `self.show_progress`.
- [x] `show_progress` сделан совместимым с вызовом в форме `loop_fun(self)`.
- [x] Исправлено сравнение особей/хромосом через `Resolve.is_equal`, включая вызовы в `crossover()` и `mutation()`.
- [x] Добавлена защита от бесконечного цикла в `mutation()` через лимит попыток и исключение.
- [x] Убрано использование `np.float`, заменено на встроенный `float`.
- [x] Инициализация служебного состояния перенесена в `population_init()`, поэтому `step()` теперь безопасно вызывать отдельно от `loop()`.
- [x] В `Resolve` формализован контракт `init_environment` как abstract static method.
- [x] Улучшены `state_dict()` и `restore_from_dict()` для сохранения/восстановления большего числа полей и лучшей совместимости.
- [x] Опечатка `TradigradasException` исправлена на `TardigradasException`, при этом сохранён alias для обратной совместимости.
- [x] Выполнены smoke-проверки: импорт, `step()`, `loop()` по умолчанию, `loop()` с callback, сериализация и восстановление.

### Проверка
Smoke-тест через встроенный временный пример `SphereResolve` прошёл успешно.

## 29.03.2026
- [x] Выполнен полный архитектурный рефакторинг пакета `tardigradas`.
- [x] Монолитный `__init__.py` заменён модульной структурой.
- [x] Введены `Problem`, `Individual`, `ChromosomeSchema`, `engine.py`, `serialization.py` и подпакет `operators/`.
- [x] Обновлён README под новый API.
- [x] Проведён smoke-тест: импорт, несколько шагов алгоритма, сериализация и восстановление состояния.

### Итог
Новая архитектура стала значительно более модульной и расширяемой. Основные направления дальнейшего развития: автоматические тесты, примеры использования и настройка стратегий операторов через публичный API.